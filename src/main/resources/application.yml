spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dpsdb
    username: iodev
    password: iodev
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: 'create-drop'
    show-sql: true
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
